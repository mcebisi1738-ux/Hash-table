# Hash-table
A hash table is a data structure that stores information in key-value pairs. It uses a mathematical algorithm called a hash function to instantly map a specific key to a storage location (index). This allows for incredibly fast data lookups, additions, and removals, regardless of how much data is stored.
